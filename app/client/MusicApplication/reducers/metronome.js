import * as types from '../constants/actionTypes';

export const getMetronome = state => ({ metronome: state.metronome });

const metronome = (state = {} , action) => {
  return state;
};

export default metronome;
